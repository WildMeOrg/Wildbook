/*  tools that you might want to do to images (aka MediaAssets) */

var imageUtilities = {
    //this will really detach the annotation, but takes MA id
    encounterDetachMediaAsset: function(mid, callback) {
    },

    //pops up ui for uploading MediaAsset as well
    encounterReplaceMediaAsset: function(mid, callback) {
    },

};

