<?xml version="1.0" encoding="UTF-8"?>
<Configuration status="WARN" monitorInterval="30">
    <!--
    status="WARN" - Internal Log4j2 logging level
    monitorInterval="30" - Auto-reload config every 30 seconds if changed
    -->

    <Properties>
        <!-- Environment variables with defaults -->
        <Property name="LOG_DIR">${sys:catalina.base:-/opt/tomcat}/logs</Property>
        <Property name="SERVICE_NAME">${env:SERVICE_NAME:-wildbook}</Property>
        <Property name="ENVIRONMENT">${env:ENVIRONMENT:-production}</Property>
        <Property name="DOMAIN_NAME">${env:DOMAIN_NAME:-unknown}</Property>
        <Property name="HOSTNAME">${env:HOSTNAME:-localhost}</Property>

        <!-- Log patterns -->
        <Property name="CONSOLE_PATTERN">%d{yyyy-MM-dd HH:mm:ss.SSS} [%t] %-5level %logger{36} - %msg%n</Property>
    </Properties>

    <Appenders>
        <!-- Console Appender - Useful for Docker logs -->
        <Console name="Console" target="SYSTEM_OUT">
            <PatternLayout pattern="${CONSOLE_PATTERN}"/>
            <ThresholdFilter level="INFO" onMatch="ACCEPT" onMismatch="DENY"/>
        </Console>

        <!-- JSON File Appender - PRIMARY for Alloy/Loki -->
        <RollingFile name="JsonFile"
                     fileName="${LOG_DIR}/application.json"
                     filePattern="${LOG_DIR}/application-%d{yyyy-MM-dd}-%i.json.gz"
                     createOnDemand="false">

            <!-- JSON Template Layout - Most efficient for Loki -->
            <JsonTemplateLayout eventTemplateUri="classpath:EcsLayout.json">
                <!-- Add custom fields for Loki labels -->
                <EventTemplateAdditionalField
                        key="service"
                        value="${SERVICE_NAME}"/>
                <EventTemplateAdditionalField
                        key="environment"
                        value="${ENVIRONMENT}"/>
                <EventTemplateAdditionalField
                        key="domain"
                        value="${DOMAIN_NAME}"/>
                <EventTemplateAdditionalField
                        key="host"
                        value="${HOSTNAME}"/>
            </JsonTemplateLayout>

            <!-- Rollover Policies -->
            <Policies>
                <TimeBasedTriggeringPolicy interval="1" modulate="true"/>
                <SizeBasedTriggeringPolicy size="100 MB"/>
            </Policies>

            <!-- Keep last 30 days of logs -->
            <DefaultRolloverStrategy max="30">
                <Delete basePath="${LOG_DIR}" maxDepth="1">
                    <IfFileName glob="application-*.json.gz"/>
                    <IfLastModified age="30d"/>
                </Delete>
            </DefaultRolloverStrategy>
        </RollingFile>

        <!-- Alternative: Simple JSON Layout (if ECS not needed) -->
        <RollingFile name="SimpleJsonFile"
                     fileName="${LOG_DIR}/simple.json"
                     filePattern="${LOG_DIR}/simple-%d{yyyy-MM-dd}-%i.json.gz">

            <JsonLayout
                    compact="true"
                    eventEol="true"
                    properties="true"
                    stacktraceAsString="true">
                <KeyValuePair key="service" value="${SERVICE_NAME}"/>
                <KeyValuePair key="environment" value="${ENVIRONMENT}"/>
                <KeyValuePair key="domain" value="${DOMAIN_NAME}"/>
                <KeyValuePair key="host" value="${HOSTNAME}"/>
            </JsonLayout>

            <Policies>
                <TimeBasedTriggeringPolicy/>
                <SizeBasedTriggeringPolicy size="100 MB"/>
            </Policies>
            <DefaultRolloverStrategy max="30"/>
        </RollingFile>

        <!-- Error File - Separate file for errors only -->
        <RollingFile name="ErrorFile"
                     fileName="${LOG_DIR}/error.log"
                     filePattern="${LOG_DIR}/error-%d{yyyy-MM-dd}-%i.log.gz">
            <PatternLayout>
                <Pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%t] %-5level %logger{36} - %msg%n%throwable</Pattern>
            </PatternLayout>
            <ThresholdFilter level="ERROR" onMatch="ACCEPT" onMismatch="DENY"/>
            <Policies>
                <TimeBasedTriggeringPolicy/>
                <SizeBasedTriggeringPolicy size="50 MB"/>
            </Policies>
            <DefaultRolloverStrategy max="30"/>
        </RollingFile>

        <!-- Async Wrapper for Performance -->
        <Async name="AsyncJson" includeLocation="false">
            <AppenderRef ref="JsonFile"/>
        </Async>

        <Async name="AsyncError" includeLocation="true">
            <AppenderRef ref="ErrorFile"/>
        </Async>
    </Appenders>

    <Loggers>
        <!-- Application Loggers -->
        <Logger name="org.wildbook" level="INFO" additivity="false">
            <AppenderRef ref="AsyncJson"/>
            <AppenderRef ref="AsyncError"/>
        </Logger>

        <!-- Third-party Library Loggers -->
        <Logger name="org.hibernate" level="WARN" additivity="false">
            <AppenderRef ref="AsyncJson"/>
        </Logger>

        <Logger name="org.springframework" level="INFO" additivity="false">
            <AppenderRef ref="AsyncJson"/>
        </Logger>

        <!-- Suppress noisy loggers -->
        <Logger name="org.apache.catalina" level="WARN"/>
        <Logger name="org.apache.coyote" level="WARN"/>
        <Logger name="org.apache.tomcat" level="WARN"/>

        <!-- Root Logger -->
        <Root level="INFO">
            <AppenderRef ref="Console"/>
            <AppenderRef ref="AsyncJson"/>
            <AppenderRef ref="AsyncError"/>
        </Root>
    </Loggers>
</Configuration>
