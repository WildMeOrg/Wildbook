
import React from "react";
import { observer } from "mobx-react-lite";

export const BulkImportIdentificationProgress = observer(({ store }) => {

    return (
        <div>
            <h2>Identification Progress</h2>
        </div>
    );
})  